node_modules/
out/
dist/
release/

# Prisma
# NOTE: prisma/migrations/ (active baseline) MUST stay tracked for fresh-clone recovery
prisma/*.db
prisma/*.db-journal
prisma/*.db-wal
prisma/*.db-shm

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Operational backup (berisi data personal — jangan di-commit)
# NOTE: pola di-anchor ke root (`/backup/`) agar hanya folder backup operasional
#       di root yang di-ignore; folder source `src/main/domain/backup` &
#       `src/main/infrastructure/backup` TETAP ter-track (WO-4).
/backup/

# TypeScript
*.tsbuildinfo

# Electron Builder
electron-builder-cache/